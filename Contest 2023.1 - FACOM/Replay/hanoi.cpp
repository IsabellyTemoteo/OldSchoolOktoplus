#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef unsigned long long ull;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int x; cin >> x;
    ll resp = pow(2, x) - 1;
    cout << resp << endl;
    return 0;
}
